{
  "name": "backend",
  "version": "1.0.0",
  "description": "backend written node(express.js) and used mongodb for database",
  "type": "module",
  "dev": "nodemon -r dotenv/config --experimental-json-modules Src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Shreyank Desai",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "dependencies": {
    "prettier": "^3.1.1"
  }
}
